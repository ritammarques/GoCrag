[build]
  # O script substitui o placeholder pela env var antes de publicar
  command = "sed -i \"s|OPENWEATHER_API_KEY|$OPENWEATHER_API_KEY|g\" js/app.js"
  publish = "."

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
